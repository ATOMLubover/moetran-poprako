# 待办列表

## 翻校页面

- [x] Ctrl+T：快速复制当前翻译内容到当前校对框（Ctrl+A T 复制全部内容到校对框）。
- [x] Ctrl+A+Enter：快速确认校对整页。
- [ ] 本地持久化翻译与校对数据（IndexedDB/localStorage，同步队列与恢复逻辑）。
- [ ] Ctrl+O：切换离线模式（离线时仅使用本地数据并队列同步）。
- [x] 校对完成后：修改 marker 颜色，并将显示文本改为 `proofreadContent` || `translation`。
- [x] 添加特殊符号面板，便于插入常用符号到输入框。
- [x] 校对状态实现方式：使用 `selected` 字段来标记校对目标/状态。
- [x] 右键删除标记时需要二次确认（阻止误删）。
- [x] editor 框的异常位移问题。
- [x] 校对模式下插入 source。
- [x] editor 的缩放。
- [x] 不要让图片是圆角。
- [x] 让 editor 在页间导航时不关闭。
- [ ] 翻校页面刷新图标的统一。

## 仪表盘页面

- [x] Team 列表：加强选中效果（更明显的高亮/轮廓）。
- [ ] 已完成的 assignment：从列表中移除或过滤掉已完成项。
- [x] 修复 ProjectList 按时间搜索无法取消的问题（清除/重置过滤器）。
- [x] 成员列表：显示最后一次 assignment 时间戳（用于排序/显示）。
- [x] 便捷的项目集筛选。
- [ ] 只有登记的汉化组才可以使用 PopRaKo 的信息。
- [ ] 兼容未被 PopRaKo 支持的 ProjectSet。

## 项目详情页面

- [x] 在 detail 中显示预览图片。
