# 待办列表

- [ ] Ctrl+T：快速复制当前翻译内容到当前校对框（Ctrl+A T 复制全部内容到校对框）。
- [ ] Ctrl+Alt+Enter（或 Ctrl+A Enter）：快速确认校对整页。
- [ ] Ctrl+Q：进入快速校对模式（Tab 经过去的条目自动确认）。
- [ ] 本地持久化翻译与校对数据（IndexedDB/localStorage，同步队列与恢复逻辑）。
- [ ] Ctrl+O：切换离线模式（离线时仅使用本地数据并队列同步）。
- [ ] Team 列表：加强选中效果（更明显的高亮/轮廓）。
- [ ] 校对完成后：修改 marker 颜色，并将显示文本改为 `proofreadContent`。
- [ ] 已完成的 assignment：从列表中移除或过滤掉已完成项。
- [ ] 长时间运行时展示 warmup/心跳提示（背景预热提示与进度提示）。
- [ ] 修复 ProjectList 按时间搜索无法取消的问题（清除/重置过滤器）。
- [ ] 成员列表：显示最后一次 assignment 时间戳（用于排序/显示）。
- [ ] 添加特殊符号面板，便于插入常用符号到输入框。
- [ ] 校对状态实现方式：使用 `selected` 字段来标记校对目标/状态。
- [ ] 校对模式：为当前正在校对的源添加可视化边框（框选效果）。
- [ ] 右键删除标记时需要二次确认（阻止误删）。
- [ ] 在 detail 中显示预览图片。
- [ ] 校对模式下插入 source。
- [ ] editor 框的异常位移问题。
- [ ] 便捷的项目集筛选。
